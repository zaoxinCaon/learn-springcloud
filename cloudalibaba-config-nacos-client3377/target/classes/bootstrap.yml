# nacos配置
server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.160.10:8849 #Nacos服务注册中心地址
      config:
        server-addr: 192.168.160.10:8849 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
#        group: DEV_GROUP  #指定分组
        namespace: 0ff16bf4-66d0-482b-8d49-b91606e559ce #指定命名空间


  # ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}







