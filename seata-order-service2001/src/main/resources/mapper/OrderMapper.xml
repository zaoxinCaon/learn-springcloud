<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rwx.springcloud.alibaba.dao.OrderDao">

    <resultMap id="BeanResultMap" type="com.rwx.springcloud.alibaba.domain.Order">
        <id column="id" property="user_id" jdbcType="BIGINT"/>
        <result column="user_id" property="userID" jdbcType="BIGINT"/>
        <result column="product_id" property="productId" jdbcType="BIGINT"/>
        <result column="count" property="count" jdbcType="INTEGER"/>
        <result column="money" property="money" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
    </resultMap>
    <select id="selectById" parameterType="Long" resultMap="BeanResultMap" >
        SELECT * FROM `t_order`
        WHERE id = #{id};
    </select>

    <insert id="create" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `t_order` (`id`, `user_id`, `product_id`, `count`, `money`, `status`)
        VALUES (NULL, #{userId},#{productId},#{count},#{money},0);
    </insert>

    <update id="update">
        UPDATE `t_order`
        SET status = #{status}
        WHERE id = #{id};
    </update>

</mapper>